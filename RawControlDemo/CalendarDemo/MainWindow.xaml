<Window x:Class="CalendarDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalendarDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="600">
    <Grid>
        <Calendar x:Name="datePicker" SelectionMode="SingleDate" Grid.Row="0">
            
        </Calendar>

        <Calendar x:Name="dateRangePicker" SelectionMode="SingleRange"  Grid.Row="1">
            
        </Calendar>

        <Calendar x:Name="dateMultiRangePicker" SelectionMode="MultipleRange"  Grid.Row="2">

        </Calendar>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
